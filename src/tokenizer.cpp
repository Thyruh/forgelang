#include<string>

std::string tokenizer(std::string contents) {
  // temporal for tests:
  std::string contentsTemp = "int main\nreturn 8;";
  printf("%s", contentsTemp.c_str());

  return contents;
}
