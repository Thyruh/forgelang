const nebula:string="This is a simple string";
mut echo:string="Hello";
const exit_code:u32=0;
println(nebula);
mut glyph:char='4';
const size:usize = 100;
size = 10; // TODO: make sure this does not work
// This is a comment
const banner:string="Hello, Forgelang!";
mut bravo:i32 = 10;
bravo = 17;
// println(banner); // Comments also work from here
echo="new";
exit(0);
