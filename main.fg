const nebula:string="This is a simple string";
mut echo:string="Hello";
const exit_code:u32=0;
println(nebula);
mut glyph:char='4';
mut size:usize=100;
size = 10;
writeln(nebula);
writeln(nebula);
const banner:string="Hello, Forgelang!";
mut new:string = banner;
new = "Hello, World!";
println(new);
mut bravo:usize=69;
bravo = 3;
print(new);
exit(exit_code);
